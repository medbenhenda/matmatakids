<form method="post" action="{{ path('proposing_transaction_delete', {'id': proposing_transaction.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ proposing_transaction.id) }}">
    <button class="btn">Delete</button>
</form>
